{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Debug Current File",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}/patriot_center_backend",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/patriot_center_backend"
      }
    }
  ]
}